


import Menu from "../../components/layout/menu/menu";
import MainWindow from "../../components/layout/main-window/main-window";

const Notes = () => {
    return (
        <div style={{display: "flex", height: "100vh"}}>
            <Menu></Menu>
            <MainWindow></MainWindow>
        </div>
    );
}

export default Notes;